<Window x:Class="CoreAIpet.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:CoreAIpet.Desktop.Controls"
        xmlns:rendering="clr-namespace:CoreAIpet.Desktop.Live2D.Rendering"
        Title="CoreAIpet"
        Width="200" Height="280"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Grid>
        <!-- Canvas 占位符（Live2D 未加载时显示） -->
        <controls:PetCharacterControl x:Name="CharacterDisplay"
                                      Width="200"
                                      Height="280"
                                      Visibility="Visible"/>

        <!-- Live2D 渲染宿主（Live2D 加载后显示） -->
        <rendering:Live2DHostControl x:Name="Live2DDisplay"
                                     Width="200"
                                     Height="280"
                                     Visibility="Visible"/>

        <!-- 径向菜单 -->
        <controls:RadialMenuControl x:Name="RadialMenu"
            Width="100" Height="100"
            Visibility="Collapsed"
            HorizontalAlignment="Center"
            VerticalAlignment="Center" />
    </Grid>
</Window>
