<Window x:Class="CoreAIpet.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:CoreAIpet.Desktop.Controls"
        Title="CoreAIpet"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual">

    <Grid x:Name="RootGrid" Width="200" Height="300">
        <!-- Live2D 角色宿主区域 -->
        <Border x:Name="Live2DHostArea" Background="Transparent" />

        <!-- 径向菜单 -->
        <controls:RadialMenuControl x:Name="RadialMenu"
            Width="200" Height="200"
            Visibility="Collapsed"
            HorizontalAlignment="Center"
            VerticalAlignment="Center" />
    </Grid>
</Window>
