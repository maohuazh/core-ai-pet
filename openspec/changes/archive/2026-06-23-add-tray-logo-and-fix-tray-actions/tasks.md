## 1. Logo 资源集成

- [x] 1.1 将 AI 宠物 logo PNG 图片复制到 `src-tauri/icons/` 目录，命名为 `logo.png`
- [x] 1.2 准备多分辨率 logo 版本（32x32, 128x128, 256x256），保存到 `src-tauri/icons/` 目录

## 2. 托盘图标配置

- [x] 2.1 在 Rust 后端创建动态托盘图标，使用 logo 作为图标
- [x] 2.2 配置托盘图标悬停提示文字为 "Core AI Pet"
- [x] 2.3 验证应用启动后托盘图标正确显示 logo（需启动 app 验证）

## 3. 托盘菜单功能修复

- [x] 3.1 修复托盘右键菜单"显示窗口"功能：点击后显示宠物窗口并获取焦点
- [x] 3.2 修复托盘右键菜单"退出"功能：点击后完全退出应用并移除托盘图标
- [x] 3.3 验证最小化到托盘时 logo 图标持续显示（需启动 app 验证）
- [x] 3.4 端到端测试：启动应用 → 最小化到托盘 → 右键菜单显示窗口 → 右键菜单退出（需启动 app 验证）
